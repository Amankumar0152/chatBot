spring.application.name=chatbot
# MySQL DB (change user/password)
spring.datasource.url=jdbc:mysql://localhost:3306/chatbot?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=AWS4sdlish

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Gemini API (store your key here)
gemini.api.key=YOUR_GEMINI_API_KEY
# gemini.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent
